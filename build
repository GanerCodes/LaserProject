#!/bin/sh

set -e ; cd "${0%/*}"

WEBTEMPLATER_PATH="$(realpath ./Building/site_builder/site_builder)"
CPY_PATH="$(realpath ./Building/cpy/cpy_tool)"

rm -r ./dest || :
sh "${WEBTEMPLATER_PATH}" ./LaserProject ./dest
cd dest

clear
sh "${CPY_PATH}" ./server.cpy